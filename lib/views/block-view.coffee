if module?.exports?
  # Add any requires to JCD
  JCD = {}

class BlockView extends Backbone.View
  
if module?.exports?
  module.exports.BlockView = BlockView
else if window?
  window.JCD.BlockView = BlockView
