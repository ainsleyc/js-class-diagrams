if module?.exports?
  # Add any requires to JCD
  JCD = {}

class GeneralizationView extends Backbone.View
  

if module?.exports?
  module.exports = GeneralizationView
else if window?
  window.JCD.GeneralizationView = GeneralizationView
