if module?.exports?
  # Add any requires to JCD
  JCD = {}

class RelationView extends Backbone.View
  

if module?.exports?
  module.exports = RelationView
else if window?
  window.JCD.RelationView = RelationView
