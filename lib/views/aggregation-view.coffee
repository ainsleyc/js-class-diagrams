if module?.exports?
  # Add any requires to JCD
  JCD = {}

class AggregationView extends Backbone.View
  

if module?.exports?
  module.exports = AggregationView
else if window?
  window.JCD.AggregationView = AggregationView
