if module?.exports?
  # Add any requires to JCD
  JCD = {}

class RealizationView extends Backbone.View
  

if module?.exports?
  module.exports = RealizationView
else if window?
  window.JCD.RealizationView = RealizationView
