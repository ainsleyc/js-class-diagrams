if module?.exports?
  # Add any requires to JCD
  JCD = {}

class RelationCollection extends Backbone.Collection
  

if module?.exports?
  module.exports = RelationCollection
else if window?
  window.JCD.RelationCollection = RelationCollection
