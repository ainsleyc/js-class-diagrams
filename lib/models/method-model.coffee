if module?.exports?
  # Add any requires to JCD
  JCD = {}

class MethodModel extends Backbone.Model
  

if module?.exports?
  module.exports = MethodModel
else if window?
  window.JCD.MethodModel = MethodModel
